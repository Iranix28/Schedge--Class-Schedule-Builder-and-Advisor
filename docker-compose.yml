version: "3.9"
services:
  backend:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app # ← live-mount your local code
    command: uvicorn backend.main:app --host 0.0.0.0 --port 5000 --reload
